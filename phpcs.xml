<?xml version="1.0"?>
<ruleset name="Project">
    <description>Project Coding Standards</description>

    <!-- Extend Apiera standards -->
    <rule ref="vendor/apiera/php-standards/phpcs/ruleset.xml"/>

    <!-- Define source paths -->
    <file>src/</file>
    <file>tests/</file>

    <!-- Exclude paths if needed -->
    <exclude-pattern>*/vendor/*</exclude-pattern>
    <exclude-pattern>*/cache/*</exclude-pattern>
</ruleset>